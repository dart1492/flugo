import 'package:flutter/material.dart';

/// Colors used in the app
class AppColors {
  /// Highlighted pink color - for accents, etc.
  static const Color highlightedViolet = Color(0xFF7461FF);

  /// Grey color for background elements, hint text, etc.
  static const Color grey = Color(0xFF8F8E97);

  /// Darker grey for borders
  static const Color darkerGrey = Color(0xFF383640);

  /// For background
  static const Color darkBlue = Color(0xFF18171F);

  /// Darker violet color
  static const Color lightBlue = Color(0xFF1D1C23);

  /// Highlighted pink color - for accents, etc.
  static const Color plainWhite = Colors.white;

  /// Light red color, usually for highlighting errors
  static const Color highlightedRed = Color(0xFFFF5858);

  /// Light red color, usually for highlighting errors
  static const Color lightRed = Color.fromARGB(255, 238, 116, 107);

  static const Color lightGreen = Color.fromARGB(212, 125, 255, 130);
}
